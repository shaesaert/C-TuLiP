//
// Autogenerated based on code by L. J. Feldstein
//

#include <stdio.h>
#include "cimple_controller.h"

void ACT_0x3F408380(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F408380;
    printf("Computing control sequence to go from cell %d to cell 0x3F408380...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F408290(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F408290;
    printf("Computing control sequence to go from cell %d to cell 0x3F408290...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F40AC50(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F40AC50;
    printf("Computing control sequence to go from cell %d to cell 0x3F40AC50...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F408440(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F408440;
    printf("Computing control sequence to go from cell %d to cell 0x3F408440...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F40DBD0(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F40DBD0;
    printf("Computing control sequence to go from cell %d to cell 0x3F40DBD0...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F4081D0(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F4081D0;
    printf("Computing control sequence to go from cell %d to cell 0x3F4081D0...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F407F90(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F407F90;
    printf("Computing control sequence to go from cell %d to cell 0x3F407F90...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F408110(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F408110;
    printf("Computing control sequence to go from cell %d to cell 0x3F408110...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F408350(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F408350;
    printf("Computing control sequence to go from cell %d to cell 0x3F408350...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F40A9D0(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F40A9D0;
    printf("Computing control sequence to go from cell %d to cell 0x3F40A9D0...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F408050(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F408050;
    printf("Computing control sequence to go from cell %d to cell 0x3F408050...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F40D770(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F40D770;
    printf("Computing control sequence to go from cell %d to cell 0x3F40D770...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F40AF40(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F40AF40;
    printf("Computing control sequence to go from cell %d to cell 0x3F40AF40...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F40B4F0(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F40B4F0;
    printf("Computing control sequence to go from cell %d to cell 0x3F40B4F0...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F40B150(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F40B150;
    printf("Computing control sequence to go from cell %d to cell 0x3F40B150...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F40B400(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F40B400;
    printf("Computing control sequence to go from cell %d to cell 0x3F40B400...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F40C9C0(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F40C9C0;
    printf("Computing control sequence to go from cell %d to cell 0x3F40C9C0...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F40B030(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F40B030;
    printf("Computing control sequence to go from cell %d to cell 0x3F40B030...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F40B340(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F40B340;
    printf("Computing control sequence to go from cell %d to cell 0x3F40B340...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F40B430(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F40B430;
    printf("Computing control sequence to go from cell %d to cell 0x3F40B430...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F40B620(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F40B620;
    printf("Computing control sequence to go from cell %d to cell 0x3F40B620...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F4099E0(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F4099E0;
    printf("Computing control sequence to go from cell %d to cell 0x3F4099E0...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F40BA30(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F40BA30;
    printf("Computing control sequence to go from cell %d to cell 0x3F40BA30...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F40B0F0(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F40B0F0;
    printf("Computing control sequence to go from cell %d to cell 0x3F40B0F0...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F409F30(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F409F30;
    printf("Computing control sequence to go from cell %d to cell 0x3F409F30...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F40B310(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F40B310;
    printf("Computing control sequence to go from cell %d to cell 0x3F40B310...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F40AEE0(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F40AEE0;
    printf("Computing control sequence to go from cell %d to cell 0x3F40AEE0...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F40AFD0(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F40AFD0;
    printf("Computing control sequence to go from cell %d to cell 0x3F40AFD0...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F406D00(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F406D00;
    printf("Computing control sequence to go from cell %d to cell 0x3F406D00...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F4070A0(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F4070A0;
    printf("Computing control sequence to go from cell %d to cell 0x3F4070A0...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F4078B0(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F4078B0;
    printf("Computing control sequence to go from cell %d to cell 0x3F4078B0...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F406F50(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F406F50;
    printf("Computing control sequence to go from cell %d to cell 0x3F406F50...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F4077F0(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F4077F0;
    printf("Computing control sequence to go from cell %d to cell 0x3F4077F0...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F4074A0(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F4074A0;
    printf("Computing control sequence to go from cell %d to cell 0x3F4074A0...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F407250(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F407250;
    printf("Computing control sequence to go from cell %d to cell 0x3F407250...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F407160(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F407160;
    printf("Computing control sequence to go from cell %d to cell 0x3F407160...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F407700(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F407700;
    printf("Computing control sequence to go from cell %d to cell 0x3F407700...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F407220(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F407220;
    printf("Computing control sequence to go from cell %d to cell 0x3F407220...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F407310(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F407310;
    printf("Computing control sequence to go from cell %d to cell 0x3F407310...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F4073D0(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F4073D0;
    printf("Computing control sequence to go from cell %d to cell 0x3F4073D0...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F407980(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F407980;
    printf("Computing control sequence to go from cell %d to cell 0x3F407980...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F406E80(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F406E80;
    printf("Computing control sequence to go from cell %d to cell 0x3F406E80...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F407560(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F407560;
    printf("Computing control sequence to go from cell %d to cell 0x3F407560...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F407640(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F407640;
    printf("Computing control sequence to go from cell %d to cell 0x3F407640...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F40D0F0(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F40D0F0;
    printf("Computing control sequence to go from cell %d to cell 0x3F40D0F0...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F40CC30(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F40CC30;
    printf("Computing control sequence to go from cell %d to cell 0x3F40CC30...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F40CB50(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F40CB50;
    printf("Computing control sequence to go from cell %d to cell 0x3F40CB50...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F40CDB0(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F40CDB0;
    printf("Computing control sequence to go from cell %d to cell 0x3F40CDB0...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F40D020(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F40D020;
    printf("Computing control sequence to go from cell %d to cell 0x3F40D020...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F40CEA0(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F40CEA0;
    printf("Computing control sequence to go from cell %d to cell 0x3F40CEA0...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F407BF0(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F407BF0;
    printf("Computing control sequence to go from cell %d to cell 0x3F407BF0...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F40BE60(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F40BE60;
    printf("Computing control sequence to go from cell %d to cell 0x3F40BE60...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F40A070(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F40A070;
    printf("Computing control sequence to go from cell %d to cell 0x3F40A070...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F40AD40(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F40AD40;
    printf("Computing control sequence to go from cell %d to cell 0x3F40AD40...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F700870(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F700870;
    printf("Computing control sequence to go from cell %d to cell 0x3F700870...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F40BD40(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F40BD40;
    printf("Computing control sequence to go from cell %d to cell 0x3F40BD40...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F40D1D0(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F40D1D0;
    printf("Computing control sequence to go from cell %d to cell 0x3F40D1D0...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F40CF30(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F40CF30;
    printf("Computing control sequence to go from cell %d to cell 0x3F40CF30...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F407B30(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F407B30;
    printf("Computing control sequence to go from cell %d to cell 0x3F407B30...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F40BF20(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F40BF20;
    printf("Computing control sequence to go from cell %d to cell 0x3F40BF20...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F40D3B0(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F40D3B0;
    printf("Computing control sequence to go from cell %d to cell 0x3F40D3B0...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F406CA0(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F406CA0;
    printf("Computing control sequence to go from cell %d to cell 0x3F406CA0...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F40D6B0(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F40D6B0;
    printf("Computing control sequence to go from cell %d to cell 0x3F40D6B0...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F40D4E0(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F40D4E0;
    printf("Computing control sequence to go from cell %d to cell 0x3F40D4E0...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F4087B0(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F4087B0;
    printf("Computing control sequence to go from cell %d to cell 0x3F4087B0...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F409950(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F409950;
    printf("Computing control sequence to go from cell %d to cell 0x3F409950...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F409980(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F409980;
    printf("Computing control sequence to go from cell %d to cell 0x3F409980...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F409590(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F409590;
    printf("Computing control sequence to go from cell %d to cell 0x3F409590...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F409890(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F409890;
    printf("Computing control sequence to go from cell %d to cell 0x3F409890...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F4086F0(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F4086F0;
    printf("Computing control sequence to go from cell %d to cell 0x3F4086F0...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F409650(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F409650;
    printf("Computing control sequence to go from cell %d to cell 0x3F409650...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F409A40(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F409A40;
    printf("Computing control sequence to go from cell %d to cell 0x3F409A40...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F409EA0(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F409EA0;
    printf("Computing control sequence to go from cell %d to cell 0x3F409EA0...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F40A850(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F40A850;
    printf("Computing control sequence to go from cell %d to cell 0x3F40A850...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F40AD10(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F40AD10;
    printf("Computing control sequence to go from cell %d to cell 0x3F40AD10...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F40D950(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F40D950;
    printf("Computing control sequence to go from cell %d to cell 0x3F40D950...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F40AB80(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F40AB80;
    printf("Computing control sequence to go from cell %d to cell 0x3F40AB80...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F409460(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F409460;
    printf("Computing control sequence to go from cell %d to cell 0x3F409460...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F4092B0(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F4092B0;
    printf("Computing control sequence to go from cell %d to cell 0x3F4092B0...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F409070(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F409070;
    printf("Computing control sequence to go from cell %d to cell 0x3F409070...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F409370(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F409370;
    printf("Computing control sequence to go from cell %d to cell 0x3F409370...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F4093A0(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F4093A0;
    printf("Computing control sequence to go from cell %d to cell 0x3F4093A0...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F408FB0(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F408FB0;
    printf("Computing control sequence to go from cell %d to cell 0x3F408FB0...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F40C7B0(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F40C7B0;
    printf("Computing control sequence to go from cell %d to cell 0x3F40C7B0...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F40C960(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F40C960;
    printf("Computing control sequence to go from cell %d to cell 0x3F40C960...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F40C870(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F40C870;
    printf("Computing control sequence to go from cell %d to cell 0x3F40C870...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F40C3C0(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F40C3C0;
    printf("Computing control sequence to go from cell %d to cell 0x3F40C3C0...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F40BDD0(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F40BDD0;
    printf("Computing control sequence to go from cell %d to cell 0x3F40BDD0...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F40C6C0(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F40C6C0;
    printf("Computing control sequence to go from cell %d to cell 0x3F40C6C0...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F40C0F0(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F40C0F0;
    printf("Computing control sequence to go from cell %d to cell 0x3F40C0F0...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F40C480(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F40C480;
    printf("Computing control sequence to go from cell %d to cell 0x3F40C480...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F40BFE0(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F40BFE0;
    printf("Computing control sequence to go from cell %d to cell 0x3F40BFE0...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F40C240(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F40C240;
    printf("Computing control sequence to go from cell %d to cell 0x3F40C240...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F408E80(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F408E80;
    printf("Computing control sequence to go from cell %d to cell 0x3F408E80...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F408B50(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F408B50;
    printf("Computing control sequence to go from cell %d to cell 0x3F408B50...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F408C10(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F408C10;
    printf("Computing control sequence to go from cell %d to cell 0x3F408C10...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F409C40(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F409C40;
    printf("Computing control sequence to go from cell %d to cell 0x3F409C40...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F409D40(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F409D40;
    printf("Computing control sequence to go from cell %d to cell 0x3F409D40...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F408CD0(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F408CD0;
    printf("Computing control sequence to go from cell %d to cell 0x3F408CD0...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F408DC0(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F408DC0;
    printf("Computing control sequence to go from cell %d to cell 0x3F408DC0...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F409F90(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F409F90;
    printf("Computing control sequence to go from cell %d to cell 0x3F409F90...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F408D90(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F408D90;
    printf("Computing control sequence to go from cell %d to cell 0x3F408D90...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F40A4C0(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F40A4C0;
    printf("Computing control sequence to go from cell %d to cell 0x3F40A4C0...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F40A0A0(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F40A0A0;
    printf("Computing control sequence to go from cell %d to cell 0x3F40A0A0...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F40A3E0(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F40A3E0;
    printf("Computing control sequence to go from cell %d to cell 0x3F40A3E0...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F40A250(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F40A250;
    printf("Computing control sequence to go from cell %d to cell 0x3F40A250...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F40A160(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F40A160;
    printf("Computing control sequence to go from cell %d to cell 0x3F40A160...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F40A220(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F40A220;
    printf("Computing control sequence to go from cell %d to cell 0x3F40A220...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F409E70(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F409E70;
    printf("Computing control sequence to go from cell %d to cell 0x3F409E70...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F40A580(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F40A580;
    printf("Computing control sequence to go from cell %d to cell 0x3F40A580...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F40A310(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F40A310;
    printf("Computing control sequence to go from cell %d to cell 0x3F40A310...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F40CF00(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F40CF00;
    printf("Computing control sequence to go from cell %d to cell 0x3F40CF00...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F40BE30(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F40BE30;
    printf("Computing control sequence to go from cell %d to cell 0x3F40BE30...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F407DA0(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F407DA0;
    printf("Computing control sequence to go from cell %d to cell 0x3F407DA0...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F407E60(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F407E60;
    printf("Computing control sequence to go from cell %d to cell 0x3F407E60...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F407D70(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F407D70;
    printf("Computing control sequence to go from cell %d to cell 0x3F407D70...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F407CB0(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F407CB0;
    printf("Computing control sequence to go from cell %d to cell 0x3F407CB0...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F406FB0(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F406FB0;
    printf("Computing control sequence to go from cell %d to cell 0x3F406FB0...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F406D90(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F406D90;
    printf("Computing control sequence to go from cell %d to cell 0x3F406D90...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F40B820(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F40B820;
    printf("Computing control sequence to go from cell %d to cell 0x3F40B820...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F700CD0(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F700CD0;
    printf("Computing control sequence to go from cell %d to cell 0x3F700CD0...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F700540(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F700540;
    printf("Computing control sequence to go from cell %d to cell 0x3F700540...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F40B5B0(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F40B5B0;
    printf("Computing control sequence to go from cell %d to cell 0x3F40B5B0...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F700240(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F700240;
    printf("Computing control sequence to go from cell %d to cell 0x3F700240...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F700970(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F700970;
    printf("Computing control sequence to go from cell %d to cell 0x3F700970...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F40B9D0(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F40B9D0;
    printf("Computing control sequence to go from cell %d to cell 0x3F40B9D0...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F700C00(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F700C00;
    printf("Computing control sequence to go from cell %d to cell 0x3F700C00...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F40B8E0(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F40B8E0;
    printf("Computing control sequence to go from cell %d to cell 0x3F40B8E0...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F700150(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F700150;
    printf("Computing control sequence to go from cell %d to cell 0x3F700150...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F40B680(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F40B680;
    printf("Computing control sequence to go from cell %d to cell 0x3F40B680...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F40D740(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F40D740;
    printf("Computing control sequence to go from cell %d to cell 0x3F40D740...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F40D710(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F40D710;
    printf("Computing control sequence to go from cell %d to cell 0x3F40D710...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F40E6E0(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F40E6E0;
    printf("Computing control sequence to go from cell %d to cell 0x3F40E6E0...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F40DC90(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F40DC90;
    printf("Computing control sequence to go from cell %d to cell 0x3F40DC90...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F40DD80(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F40DD80;
    printf("Computing control sequence to go from cell %d to cell 0x3F40DD80...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F40D880(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F40D880;
    printf("Computing control sequence to go from cell %d to cell 0x3F40D880...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F40E330(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F40E330;
    printf("Computing control sequence to go from cell %d to cell 0x3F40E330...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F40D920(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F40D920;
    printf("Computing control sequence to go from cell %d to cell 0x3F40D920...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F40CA80(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F40CA80;
    printf("Computing control sequence to go from cell %d to cell 0x3F40CA80...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F40D600(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F40D600;
    printf("Computing control sequence to go from cell %d to cell 0x3F40D600...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F40AE80(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F40AE80;
    printf("Computing control sequence to go from cell %d to cell 0x3F40AE80...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F40CCC0(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F40CCC0;
    printf("Computing control sequence to go from cell %d to cell 0x3F40CCC0...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F40AFA0(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F40AFA0;
    printf("Computing control sequence to go from cell %d to cell 0x3F40AFA0...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F700B40(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F700B40;
    printf("Computing control sequence to go from cell %d to cell 0x3F700B40...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F700300(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F700300;
    printf("Computing control sequence to go from cell %d to cell 0x3F700300...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F700810(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F700810;
    printf("Computing control sequence to go from cell %d to cell 0x3F700810...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F700180(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F700180;
    printf("Computing control sequence to go from cell %d to cell 0x3F700180...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F700660(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F700660;
    printf("Computing control sequence to go from cell %d to cell 0x3F700660...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F7003D0(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F7003D0;
    printf("Computing control sequence to go from cell %d to cell 0x3F7003D0...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F700A50(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F700A50;
    printf("Computing control sequence to go from cell %d to cell 0x3F700A50...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F700090(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F700090;
    printf("Computing control sequence to go from cell %d to cell 0x3F700090...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F408570(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F408570;
    printf("Computing control sequence to go from cell %d to cell 0x3F408570...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F408A20(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F408A20;
    printf("Computing control sequence to go from cell %d to cell 0x3F408A20...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F408870(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F408870;
    printf("Computing control sequence to go from cell %d to cell 0x3F408870...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F408930(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F408930;
    printf("Computing control sequence to go from cell %d to cell 0x3F408930...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F408960(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F408960;
    printf("Computing control sequence to go from cell %d to cell 0x3F408960...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F408630(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F408630;
    printf("Computing control sequence to go from cell %d to cell 0x3F408630...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F40B740(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F40B740;
    printf("Computing control sequence to go from cell %d to cell 0x3F40B740...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F40BA90(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F40BA90;
    printf("Computing control sequence to go from cell %d to cell 0x3F40BA90...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F40BB60(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F40BB60;
    printf("Computing control sequence to go from cell %d to cell 0x3F40BB60...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F409CE0(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F409CE0;
    printf("Computing control sequence to go from cell %d to cell 0x3F409CE0...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

void ACT_0x3F409B00(current_state * now, discrete_dynamics * d_dyn, system_dynamics * s_dyn, cost_function * f_cost){
    int target_cell =0x3F409B00;
    printf("Computing control sequence to go from cell %d to cell 0x3F409B00...", (*now).current_cell);
    fflush(stdout);

    gsl_matrix * u = gsl_matrix_alloc(now->x->size, d_dyn->time_horizon);
    get_input(u, now, d_dyn, s_dyn, target_cell, f_cost);
    printf("Applying it...");
    fflush(stdout);
    apply_control(now->x, u, s_dyn->A, s_dyn->B);
    printf("New state:");
    gsl_vector_print(now->x, "now->");
    fflush(stdout);
    // Clean up!
    gsl_matrix_free(u);
}

